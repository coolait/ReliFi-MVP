[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python api_server.py"
healthcheckPath = "/api/health"
healthcheckTimeout = 100

# Explicitly set the entry point to prevent Railway from auto-detecting main.py
[deploy.entrypoint]
command = "python api_server.py"

